"""Shared fixtures for convergence settings tests.

This module is intentionally NOT named conftest.py so it does not conflict with
the existing conftest.py in the same directory. Fixtures are imported explicitly
in the test modules that need them.
"""
